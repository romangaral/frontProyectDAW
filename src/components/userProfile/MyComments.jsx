import React, { Fragment } from 'react';
import '../../styles/userProfile.css';

function MyComments({ credentials }) {
    console.log(credentials);
    return (
        <Fragment>
            <h1>Desde MyComments</h1>
        </Fragment>
    );
}

export default MyComments;